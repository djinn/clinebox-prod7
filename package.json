{
  "name": "clinebox-prod7",
  "version": "1.0.0",
  "description": "Deploy a globally distributed AI coding workspace to Cloudflare in minutes.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.93.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.4"
  }
}
